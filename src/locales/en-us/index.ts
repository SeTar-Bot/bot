import { localeBase } from "../../../types/localeSchema";
import en_usErrors from "./error";
import en_usReplies from "./reply";

const en_us: localeBase = {
    reply: en_usReplies,
    error: en_usErrors
    
};

export default en_us;