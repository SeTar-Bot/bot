import { SCExtractorResult } from "../typings/soundcloud";
declare const validator: (url: string) => boolean;
declare function extractor(url: string): SCExtractorResult;
export { validator as SoundcloudValidator, extractor as SoundcloudExtractor };
