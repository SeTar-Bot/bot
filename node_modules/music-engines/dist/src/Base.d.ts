export declare class Base {
    readonly platform: string;
    constructor(platform: string);
    isYoutube(): boolean;
    isSoundcloud(): boolean;
    isSpotify(): boolean;
    isDeezer(): boolean;
}
