"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DeezerValidator = exports.DeezerExtractor = void 0;
function extractor(input) {
    const myRegexp = new RegExp("http[s]*\:[\/]{2}[^.]+[.]deezer.com\/us\/([a-zA-Z-_]+)\/([0-9]+)", "gm");
    const match = myRegexp.exec(input);
    if (!match)
        return { type: 'unknown', id: 'unknwon' };
    const type = match[1] || null;
    const id = match[2] || null;
    return { type, id };
}
exports.DeezerExtractor = extractor;
function validator(url) {
    const pattern = /http[s]*\:[\/]{2}[^.]+[.]deezer.com\/us\/([a-zA-Z-_]+)\/([0-9]+)/;
    return pattern.test(url);
}
exports.DeezerValidator = validator;
