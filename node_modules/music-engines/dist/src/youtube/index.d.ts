import { YTEngine, YTExtractorResult, YTOptions } from "../typings/youtube";
import { Video as YouTubeSRVideo, Channel as YouTubeSRChannel, Playlist as YouTubeSRPlaylist } from "youtube-sr";
import { YouTubeArtist, YouTubePlaylist, YouTubeTrack } from "./wrappers";
declare class YouTube implements YTEngine {
    validator: (input: string) => boolean;
    extractor: (url: string) => YTExtractorResult;
    constructor();
    private handleSR;
    private searchVideo;
    private searchAll;
    private searchPlaylist;
    private searchChannel;
    use(input: string, opts?: YTOptions): Promise<(YouTubeTrack | YouTubeArtist | YouTubePlaylist | YouTubeSRChannel | YouTubeSRVideo | YouTubeSRPlaylist)[]>;
}
export { YouTube };
export * as YoutubeWrappers from "./wrappers";
