import { SPExtractorResult } from "../typings/spotify";
/**
 * @param {String} url Spotify URL/ID
 * @returns {ParsedSpotifyUri} Parsed Object
 */
declare function extractor(url: string): SPExtractorResult;
/**
 * @param {String} input URL to check if it's Valid
 */
declare function validator(input: string): boolean;
export { extractor as SpotifyExtractor, validator as SpotifyValidator };
